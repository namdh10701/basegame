using UnityEngine;

namespace _Game.Features.MyShip.GridSystem
{
    public class PlacementPane : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
