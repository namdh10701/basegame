using _Base.Scripts.RPG.Entities;

namespace _Base.Scripts.RPGCommon
{
    public class AttributeInitiator
    {
        public void Initiate(Entity entity)
        {
            // if (entity is IAlive)
            // {
            //     entity.Attributes.Add(new HealthPoint());
            //     entity.Attributes.Add(new ManaPoint());
            // }
            // else if (entity is IFighter)
            // {
            //     entity.Attributes.Add(new AttackDamage());
            //     entity.Attributes.Add(new CriticalChance());
            //     entity.Attributes.Add(new CriticalDamage());
            // }
        }
    }
}